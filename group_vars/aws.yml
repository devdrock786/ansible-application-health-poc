---
application_name: health-monitor
environment_name: development
application_root: /opt/health-monitor

required_packages:
  - git
  - curl
  - unzip

application_directories:
  - /opt/health-monitor
  - /opt/health-monitor/logs
  - /opt/health-monitor/config

application_users:
  - name: healthadmin
    group: monitoring
  - name: healthreader
    group: monitoring